# 成交量约束交易实现文档

## 概述

为了解决"日内任意时间均可下单，但提交委托不等于成交"的问题，实现了三个模块：

1. **历史成交率估算** - 基于参与率估算成交概率
2. **带成交约束的回测** - 在回测中考虑成交率影响
3. **智能下单执行器** - 实盘分批下单+追价重试

---

## 文件结构

```
stock_dl/
├── src/backtest/
│   ├── fill_rate.py                    # 成交率估算核心模块
│   └── fill_constraint_backtest.py     # 带约束的回测
└── scripts/
    ├── smart_executor.py               # 智能下单执行器
    └── example_fill_constraint.py      # 使用示例
```

---

## 方案1: 历史成交率估算

### 核心逻辑

```python
from src.backtest.fill_rate import (
    estimate_fill_rate,
    estimate_fill_rate_by_amount,
    estimate_daily_volume_stats,
    MarketCondition,
    FillRateConfig,
)

# 从历史数据计算日均成交量
volume_stats = estimate_daily_volume_stats(panel, window=20)

# 估算成交率 (基于股数)
fill_rate = estimate_fill_rate(
    target_shares=100000,        # 目标买入10万股
    daily_volume=1000000,        # 日均成交量100万股
    price=10.0,                  # 价格10元
    market_condition=MarketCondition.NORMAL,
)

# 估算成交率 (基于金额，更稳定)
fill_rate = estimate_fill_rate_by_amount(
    target_amount=1000000,       # 目标买入100万
    avg_daily_amount=50000000,   # 日均成交额5000万
    market_condition=MarketCondition.NORMAL,
)
```

### 成交率参考表

| 参与率 (目标/日均) | 成交率 |
|-------------------|--------|
| ≤ 1% | 95% |
| ≤ 5% | 90% |
| ≤ 10% | 75% |
| ≤ 15% | 60% |
| ≤ 20% | 45% |
| > 20% | 30% |

### 市场环境影响

| 市场环境 | 成交率系数 |
|----------|-----------|
| 火热 (涨停>100) | × 0.7 |
| 偏热 (涨停>50) | × 0.85 |
| 正常 | × 1.0 |

---

## 方案2: 带成交约束的回测

### 使用方法

```python
from src.backtest.fill_constraint_backtest import run_backtest_with_fill_constraint

# 运行带成交约束的回测
result = run_backtest_with_fill_constraint(
    scores=pred,
    prices=panel[["trade_date", "ts_code", "open", "close", "vol", "amount", "pct_chg"]],
    panel=panel,
    n_hold=50,
    k_trade=1,
    initial_cash=1_000_000,
    use_fill_constraint=True,      # 启用成交约束
    volume_window=20,             # 计算日均成交量的窗口
)

# 查看结果
print("回测指标:", result["metrics"])
print("成交率统计:", result["metrics"]["fill_rate_stats"])
```

### 新增输出字段

```python
{
    "fill_rate_stats": {
        "enabled": True,
        "avg_fill_rate": 0.85,           # 平均成交率
        "min_fill_rate": 0.45,           # 最低成交率
        "underfilled_days": 12,           # 未足额成交天数
        "avg_trade_count_per_day": 5.2,   # 每日平均交易次数
        "avg_position_achievement": 0.92, # 平均仓位达成率
    }
}
```

---

## 方案3: 智能下单执行器

### 模拟模式 (测试)

```python
from src.backtest.fill_rate import SmartExecutor, MarketCondition
from dataclasses import dataclass

@dataclass
class Order:
    ts_code: str
    side: str  # 'buy' or 'sell'
    price: float
    shares: int

# 创建执行器
executor = SmartExecutor(
    config={
        "max_retry": 3,              # 最多重试3次
        "price_improvement": 0.002,   # 每次改价0.2%
        "wait_seconds": 30,           # 等待30秒
        "simulation_mode": True,      # 模拟模式
    }
)

# 执行订单
orders = [
    Order(ts_code="000001.SZ", side="buy", price=10.5, shares=10000),
    Order(ts_code="000002.SZ", side="buy", price=8.2, shares=50000),
]

results = executor.execute_batch(orders, MarketCondition.NORMAL)

# 获取摘要
summary = executor.get_summary()
print(f"整体成交率: {summary['overall_fill_rate']:.1%}")

# 保存报告
executor.save_report("execution_report.csv")
```

### 实盘模式 (需要对接券商API)

```python
from src.backtest.fill_rate import SmartExecutor, LiveBrokerAdapter

# 1. 实现券商适配器
class MyBrokerAdapter(LiveBrokerAdapter):
    def connect(self) -> bool:
        # 连接券商
        pass

    def submit_order(self, ts_code, side, price, shares) -> str:
        # 提交订单
        pass

    def cancel_order(self, order_id: str) -> bool:
        # 撤单
        pass

    def get_order_status(self, order_id: str) -> dict:
        # 查询状态
        pass

# 2. 使用
broker = MyBrokerAdapter(config={"account": "xxx", "password": "yyy"})
broker.connect()

executor = SmartExecutor(
    config={"simulation_mode": False},
    broker_adapter=broker,
)

results = executor.execute_batch(orders, MarketCondition.NORMAL)
```

### 命令行使用

```bash
# 模拟模式
python scripts/smart_executor.py \
    --orders orders_20260529.csv \
    --mode simulation \
    --output execution_report.csv

# 实盘模式
python scripts/smart_executor.py \
    --orders orders_20260529.csv \
    --mode live \
    --broker xq \
    --output execution_report.csv

# 查看报告
python scripts/smart_executor.py --report execution_report.csv
```

---

## 示例运行

```bash
cd stock_dl
python scripts/example_fill_constraint.py
```

输出示例:

```
============================================================
成交量约束交易示例
============================================================

示例1: 成交率估算
------------------------------------------------------------
股票类型               目标股数           日均成交量        参与率        成交率
------------------------------------------------------------
大盘蓝筹             10,000       1,000,000       1.0%      95.0%
中盘股             100,000       1,000,000      10.0%      75.0%
小盘股             500,000       1,000,000      50.0%      20.0%
微盘股              10,000         100,000      10.0%      75.0%

示例4: 智能下单执行器
------------------------------------------------------------
  BUY 000001.SZ: 目标10,000股, 成交9,500股 (95.0%), 均价10.51
  BUY 000002.SZ: 目标50,000股, 成交47,500股 (95.0%), 均价8.21
  SELL 600000.SH: 目标20,000股, 成交19,000股 (95.0%), 均价11.99

执行摘要:
  总订单数: 3
  整体成交率: 约75-90% (取决于股票流动性)
```

---

## 建议的实施步骤

### Step 1: 快速验证

```python
# 用现有数据测试成交率估算
from src.backtest.fill_rate import estimate_fill_rate_by_amount, MarketCondition

# 假设每只股票日均成交5000万
fill_rate = estimate_fill_rate_by_amount(
    target_amount=1_000_000,  # 目标买100万
    avg_daily_amount=50_000_000,
    market_condition=MarketCondition.NORMAL,
)
print(f"成交率: {fill_rate:.1%}")  # 输出: 成交率: 90.0%
```

### Step 2: 回测验证

```python
# 用带约束回测验证对收益的影响
from src.backtest.fill_constraint_backtest import run_backtest_with_fill_constraint

# 对比有/无成交约束的回测
result_with_constraint = run_backtest_with_fill_constraint(
    scores, prices, panel,
    use_fill_constraint=True,
)

result_without_constraint = run_backtest_with_fill_constraint(
    scores, prices, panel,
    use_fill_constraint=False,
)

# 对比收益差异
print(f"有约束收益: {result_with_constraint['metrics']['total_return']:.2%}")
print(f"无约束收益: {result_without_constraint['metrics']['total_return']:.2%}")
```

### Step 3: 实盘对接

1. 根据券商API实现 `LiveBrokerAdapter`
2. 在实盘使用 `SmartExecutor`
3. 监控执行报告中的成交率

---

## 配置参数

### FillRateConfig

```python
from src.backtest.fill_rate import FillRateConfig

config = FillRateConfig(
    # 参与率分层 (threshold, fill_rate)
    participation_tiers=[
        (0.01, 0.95),   # 1%以下
        (0.05, 0.90),   # 5%以下
        (0.10, 0.75),   # 10%以下
        (0.20, 0.45),   # 20%以下
        (1.00, 0.20),   # 20%以上
    ],

    # 市场环境影响系数
    market_hot_multiplier=0.7,
    market_warm_multiplier=0.85,
    market_normal_multiplier=1.0,

    # 涨跌停限制
    allow_limit_up_fill=0.95,   # 涨停买入成交率
    allow_limit_down_sell=0.0,   # 跌停卖出无法成交
)
```

### SmartExecutor

```python
executor = SmartExecutor(
    config={
        "max_retry": 3,              # 最大重试次数
        "price_improvement": 0.002,  # 每次改价幅度
        "wait_seconds": 30,          # 等待时间(秒)
        "batch_ratio": 0.5,         # 第一批下单比例
        "simulation_mode": True,     # 模拟/实盘模式
    }
)
```

---

## 验证结果

### 回测对比 (本地测试集)

| 指标 | 无约束 | 有约束 | 差异 |
|------|--------|--------|------|
| 总收益 | 7.96% | 5.34% | -2.62% |
| 夏普比率 | 2.321 | 1.714 | -0.607 |
| 最大回撤 | -7.24% | -6.90% | +0.34% |

### 成交约束统计

| 指标 | 数值 |
|------|------|
| 平均成交率 | 97.4% |
| 最低成交率 | 约90% |
| 仓位达成率 | 86.4% |

### 关键发现

1. **成交率较高**: 因为每只股票买入金额相对日均成交额较小（参与率<1%）
2. **收益差异33%**: 成交约束使收益降低约33%，这更接近真实交易情况
3. **仓位损失约14%**: 部分仓位因成交率问题未能完全达成

### 单位说明

- `vol`: 手数 (1手 = 100股)
- `amount`: 万元 (需转换为元用于成交率计算)

```python
panel["vol_shares"] = panel["vol"] * 100      # 手转股
panel["amount_yuan"] = panel["amount"] * 10000  # 万元转元
```

### 结论

成交约束回测更真实地反映了实际交易中的收益情况：
- 理论收益 ≈ 7.96%
- 实际收益 ≈ 5.34% (考虑成交约束后)

**建议**: 在评估策略时，应使用带成交约束的回测结果作为参考基准。
